# Messages Configuration

Located under `plugins/Logger/messages/` (e.g. `en_en.yml`, `de_de.yml`, `fr_fr.yml`, `zh_cn.yml`, etc.).

Allows 100% full customization of every in-game chat message, prefix, hover tooltip, and inventory GUI title.
